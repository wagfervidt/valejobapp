const execute = require('./AVERAGE.runtime');

module.exports = {
  name: 'AVERAGE',
  category: 'Statistical',
  title: 'Calculate average of a list',
  description: 'Given a list of numbers, calculates their average.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'List of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'AVERAGE([100, 20, 3])', result: 41 },
  ],
};

const execute = require('./URL.runtime');

module.exports = {
  name: 'URL',
  category: 'Utility',
  title: 'Converts text to URL',
  description: 'Converts the given text to an URL-type text. Returns `null` if the text cannot be converted to an URL.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to convert',
  }],
  analyze: () => ({ schema: { type: 'string', format: 'url' } }),
  execute,
  examples: [{
    expression: 'URL("http://foo.com/blah_blah")',
    result: 'http://foo.com/blah_blah',
  }, {
    expression: 'URL("https://www.example.com/foo/?bar=baz&inga=42&quux")',
    result: 'https://www.example.com/foo/?bar=baz&inga=42&quux',
  }, {
    expression: 'URL("http://userid@www.example.com/")',
    result: 'http://userid@www.example.com/',
  }, {
    expression: 'URL("http://142.42.1.1:8080/")',
    result: 'http://142.42.1.1:8080/',
  }],
};

const STRING = require('../utility/STRING.runtime');

module.exports = ([string]) => {
  if (typeof string === 'string') {
    return string.trim();
  }
  if (typeof string === 'number') {
    return STRING([string]);
  }
  return null;
};

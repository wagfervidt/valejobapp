const execute = require('./LEFT_STRIP.runtime');

module.exports = {
  name: 'LEFT_STRIP',
  category: 'Text',
  title: 'Takes a slice of a text',
  description: 'Returns text with number of characters stripped from the beginning of the text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text which to get a slice of text from',
  }, {
    type: 'number',
    title: 'Number',
    description: 'The numbers of characters from the beginning of the text.',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'LEFT_STRIP("foo bar", 4)', result: 'bar' },
    { expression: 'LEFT_STRIP("foo bar", 2)', result: 'o bar' },
    { expression: 'LEFT_STRIP("foo bar", -1)', result: 'foo bar' },
  ],
};

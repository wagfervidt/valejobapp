module.exports = ([value]) => {
  if (value == null) {
    return value;
  }
  const result = parseFloat(value);
  if (!Number.isFinite(result) || Number.isNaN(result)) {
    return null;
  }
  return result;
};

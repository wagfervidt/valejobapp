import { Schema } from './schemas';
import { ValidationError } from './validation';

export function getSchemaJsonSerializationErrors(schema: Schema): ValidationError[];
export function isJsonCompatibleSchema(schema: Schema): boolean;

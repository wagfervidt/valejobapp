const execute = require('./SCHEMA_DESCRIPTION.runtime');

module.exports = {
  name: 'SCHEMA_DESCRIPTION',
  category: 'Schema',
  title: 'Get schema description',
  description: 'Returns a human-readable description text for the given schema, or null if the schema does not have any description text.',
  parameters: [{
    type: 'schema',
    title: 'Schema',
    description: 'Schema from which to get the description text',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'SCHEMA_DESCRIPTION({ type: "number", description: "The price in euros" })', result: 'The price in euros' },
    { expression: 'SCHEMA_DESCRIPTION({ type: "number" })', result: null },
  ],
};

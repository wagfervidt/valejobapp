const erf = require('./ERF.runtime');

module.exports = ([num]) => {
  if (num == null) {
    return null;
  }
  if (typeof num === 'number' && Number.isFinite(num)) {
    const result = 1 - erf([num]);
    return Number.isFinite(result) ? result : null;
  }
  return null;
};

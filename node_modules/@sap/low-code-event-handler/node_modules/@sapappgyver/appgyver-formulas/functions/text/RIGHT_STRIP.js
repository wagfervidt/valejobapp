const execute = require('./RIGHT_STRIP.runtime');

module.exports = {
  name: 'RIGHT_STRIP',
  category: 'Text',
  title: 'Takes a slice of a text',
  description: 'Returns text with number of characters stripped from the end of the text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text which to get a slice from',
  }, {
    type: 'number',
    title: 'Number',
    description: 'The numbers of characters from the end text.',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'RIGHT_STRIP("foo bar", 4)', result: 'foo' },
    { expression: 'RIGHT_STRIP("foo bar", 2)', result: 'foo b' },
    { expression: 'RIGHT_STRIP("foo bar", -1)', result: 'foo bar' },
  ],
};

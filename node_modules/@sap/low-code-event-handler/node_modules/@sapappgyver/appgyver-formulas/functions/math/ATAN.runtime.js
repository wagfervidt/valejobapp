module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const atan = Math.atan(num);
    if (Number.isFinite(atan)) {
      return atan;
    }
  }
  return null;
};

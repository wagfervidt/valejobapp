module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const result = Math.ceil(Math.abs(num) + (Math.ceil(Math.abs(num)) % 2));
    return num >= 0 ? result : -result;
  }
  return null;
};

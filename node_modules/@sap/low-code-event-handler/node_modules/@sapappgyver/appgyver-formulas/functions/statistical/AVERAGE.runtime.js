module.exports = ([array]) => {
  let sum = 0;
  let count = 0;
  (array || []).forEach((value) => {
    if (typeof value === 'number') {
      sum += value;
      count += 1;
    }
  });
  return count > 0 ? sum / count : null;
};

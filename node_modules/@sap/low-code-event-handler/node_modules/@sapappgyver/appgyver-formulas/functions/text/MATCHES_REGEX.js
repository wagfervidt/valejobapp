const execute = require('./MATCHES_REGEX.runtime');

module.exports = {
  name: 'MATCHES_REGEX',
  category: 'Text',
  title: 'Check if text contains slice of text defined by regex pattern',
  description: 'Returns `true` if text is matched by the given regular expression pattern, otherwise returns `false`.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to test',
  }, {
    type: 'string',
    title: 'Regex',
    description: 'Pattern to find',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'MATCHES_REGEX("Foo Bar", "^Foo")', result: true },
    { expression: 'MATCHES_REGEX("Foo Bar", "^foo")', result: false },
    { expression: 'MATCHES_REGEX("Foo Bar", "o B")', result: true },
    { expression: 'MATCHES_REGEX("Foo Bar", "[a-zA-Z]+")', result: true },
  ],
};

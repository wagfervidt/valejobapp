const execute = require('./INTEGER.runtime');

module.exports = {
  name: 'INTEGER',
  category: 'Utility',
  title: 'Parse an integer',
  description: 'Converts the given input value to an integer number. Texts are parsed, and numbers are rounded down. If the input is already an integer, it is returned as-is. Any other values, and invalid texts, result in `null` value.',
  parameters: [{
    title: 'Value',
    description: 'Value to convert to an integer',
    anyOf: [{
      type: 'string',
    }, {
      type: 'number',
    }],
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'INTEGER("123")', result: 123 },
    { expression: 'INTEGER("123.567")', result: 123 },
    { expression: 'INTEGER("-0896")', result: -896 },
    { expression: 'INTEGER(5.67)', result: 5 },
    { expression: 'INTEGER("6e-7")', result: 0 },
  ],
};

const execute = require('./RANDOM_ARRAY.runtime');

module.exports = {
  name: 'RANDOM_ARRAY',
  category: 'Math',
  title: 'Generates a list of random numbers',
  description: 'Generates a list of (pseudo) random numbers, each between 0 (inclusive) and 1 (exclusive), i.e. 0 ≤ n < 1.',
  deterministic: false, // Return value is random
  parameters: [{
    type: 'number',
    title: 'Count',
    description: 'Count of numbers to generate',
  }],
  analyze: () => ({
    schema: {
      type: 'array',
      items: { type: 'number' },
    },
  }),
  execute,
  examples: [
    { expression: 'RANDOM_ARRAY(1)', result: [0.94954233] },
    { expression: 'RANDOM_ARRAY(3)', result: [0.42339495, 0.92366945, 0.11705423] },
  ],
};

const execute = require('./REPLACE_ONE.runtime');

module.exports = {
  name: 'REPLACE_ONE',
  category: 'Text',
  title: 'Replace the first found slice of text',
  description: 'Returns a new text where the first occurence of the given text are replaced with the given replacement text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Original text',
  }, {
    type: 'string',
    title: 'Slice of text',
    description: 'Slice of text to find',
  }, {
    type: 'string',
    title: 'New slice of text',
    description: 'Slice of text to replace the old one',
  },
  ],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'REPLACE_ONE("foo bar baz", "bar", "baz")', result: 'foo baz baz' },
    { expression: 'REPLACE_ONE("foo foo baz", "foo", "bar")', result: 'bar foo baz' },
    { expression: 'REPLACE_ONE("foo bar", "baz", "bar")', result: 'foo bar' },
  ],
};

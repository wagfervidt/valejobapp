const execute = require('./MIME_TYPE.runtime');

module.exports = {
  name: 'MIME_TYPE',
  category: 'Utility',
  title: 'MIME type',
  description: 'Returns the MIME type of the given file extension, or `null` if it cannot be determined. Also works with full paths and filenames, where it\'s assumed the text ends in an extension.',
  parameters: [{
    type: 'string',
    title: 'File extension',
    description: 'File extension',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    {
      expression: 'MIME_TYPE("json")',
      result: 'application/json',
    },
    {
      expression: 'MIME_TYPE(".json")',
      result: 'application/json',
    },
    {
      expression: 'MIME_TYPE("/cachedImages/test.png")',
      result: 'image/png',
    },
    {
      expression: 'MIME_TYPE("README.md")',
      result: 'text/markdown',
    },
  ],
};

const normSDist = require('./NORM_S_DIST.runtime');

module.exports = ([num]) => {
  if (!(typeof num === 'number' && Number.isFinite(num))) {
    return null;
  }
  const result = normSDist([num]) - 0.5;
  return Number.isFinite(result) ? result : null;
};

const execute = require('./LOWERCASE.runtime');

module.exports = {
  name: 'LOWERCASE',
  category: 'Text',
  title: 'Convert letters to lower case',
  description: 'Converts each character of the given text to lower-case letters.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to convert to lower case',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'LOWERCASE("FOOBAR")', result: 'foobar' },
    { expression: 'LOWERCASE("Foo Bar")', result: 'foo bar' },
  ],
};

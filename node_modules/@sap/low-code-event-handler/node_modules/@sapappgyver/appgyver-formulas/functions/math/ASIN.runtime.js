module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const acos = Math.asin(num);
    if (Number.isFinite(acos)) {
      return acos;
    }
  }
  return null;
};

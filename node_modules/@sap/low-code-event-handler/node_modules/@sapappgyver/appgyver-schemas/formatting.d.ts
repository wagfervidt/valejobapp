export function detectStringFormats(str: string): string[];

export interface Format {
  description: string;
  examples: string[];
  extends?: string[];
  matches: (str: string) => boolean;
  title: string;
}

export const formats: Record<string, Format>;

export function isFormatCompatible(subFormat: string, parentFormat: string): boolean;

module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const sec = 1 / Math.cos(num);
    if (Number.isFinite(sec)) {
      return sec;
    }
  }
  return null;
};

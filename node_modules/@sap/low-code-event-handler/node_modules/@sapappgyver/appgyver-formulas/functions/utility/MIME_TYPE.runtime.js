const { lookup: mimeLookup } = require('mime-types');

module.exports = ([param]) => {
  if (typeof param === 'string' && param !== '') {
    const result = mimeLookup(param);
    return result || null;
  }
  return null;
};

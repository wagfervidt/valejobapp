module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const cosec = 1 / Math.sin(num);
    if (Number.isFinite(cosec)) {
      return cosec;
    }
  }
  return null;
};

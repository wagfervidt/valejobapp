const execute = require('./IS_EVEN.runtime');

module.exports = {
  name: 'IS_EVEN',
  category: 'Math',
  title: 'Check if the number is even',
  description: 'Returns `true` if the given number is even, `false` if it\'s odd.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to check',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'IS_EVEN(0)', result: true },
    { expression: 'IS_EVEN(1)', result: false },
    { expression: 'IS_EVEN(2)', result: true },
  ],
};

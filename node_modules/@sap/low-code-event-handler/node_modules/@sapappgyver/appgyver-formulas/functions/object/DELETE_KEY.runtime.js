module.exports = ([object, prop]) => {
  if (object == null || typeof object !== 'object') {
    return null;
  }
  if (typeof prop !== 'string') {
    return object;
  }
  const result = { ...object };
  delete result[prop];
  return result;
};

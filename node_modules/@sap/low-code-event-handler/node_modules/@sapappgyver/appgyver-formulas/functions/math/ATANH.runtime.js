module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    if (num <= -1 || num >= 1) {
      return null;
    }
    const result = Math.atanh(num);
    if (Number.isFinite(result)) {
      return result;
    }
  }
  return null;
};

module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const result = Math.log(num);
    if (Number.isFinite(result)) {
      return result;
    }
  }
  return null;
};

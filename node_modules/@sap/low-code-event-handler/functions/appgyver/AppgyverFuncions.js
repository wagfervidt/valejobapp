const appgyverFunctionDefs = require('@sapappgyver/appgyver-formulas/functions');
const functions = {};

for (const functionName in appgyverFunctionDefs) {
  functions[functionName] = (...args) => appgyverFunctionDefs[functionName].execute(args);
}

module.exports = functions;
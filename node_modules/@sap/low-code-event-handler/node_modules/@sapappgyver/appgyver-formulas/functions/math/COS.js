const execute = require('./COS.runtime');

module.exports = {
  name: 'COS',
  category: 'Math',
  title: 'Calculate cosine',
  description: 'Given an angle in radians, calculates its cosine.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Angle in radians to calculate cosine for',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'COS(PI() / 3)', result: 0.5 },
    { expression: 'COS(PI() / 2)', result: 0 },
    { expression: 'COS(PI())', result: -1 },
  ],
};

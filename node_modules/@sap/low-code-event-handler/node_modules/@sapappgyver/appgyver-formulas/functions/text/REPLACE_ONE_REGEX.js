const execute = require('./REPLACE_ONE_REGEX.runtime');

module.exports = {
  name: 'REPLACE_ONE_REGEX',
  category: 'Text',
  title: 'Replace first match by regex',
  description: 'Returns a new text where the first match for the given regular expression is replaced with the given replacement text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Original text',
  }, {
    type: 'string',
    title: 'Pattern',
    description: 'Pattern to find',
  }, {
    type: 'string',
    title: 'New slice of text',
    description: 'Slice of text to replace',
  },
  ],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'REPLACE_ONE_REGEX("foo bar", "[a-zA-Z]+", "xxxx")', result: 'xxxx bar' },
    { expression: 'REPLACE_ONE_REGEX("foo foo foo", "^foo", "bar")', result: 'bar foo foo' },
    { expression: 'REPLACE_ONE_REGEX("foo foo baz", "\\\\s", "-")', result: 'foo-foo baz' },
  ],
};

module.exports = ([num]) => {
  if (num == null) {
    return null;
  }
  if (typeof num === 'number' && !Number.isNaN(num)) {
    const sign = Math.sign(num);
    return (sign === 0) ? 0 : sign; // fix return -0
  }
  return null;
};

module.exports = ([numA, numB]) => {
  if (typeof numA === 'number' && typeof numB === 'number'
    && Number.isFinite(numA) && Number.isFinite(numB)) {
    // eslint-disable-next-line no-bitwise
    return numA >> numB;
  }
  return null;
};

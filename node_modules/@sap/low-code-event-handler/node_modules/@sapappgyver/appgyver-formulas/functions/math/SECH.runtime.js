module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const sech = 1 / Math.cosh(num);
    if (Number.isFinite(sech)) {
      return sech;
    }
  }
  return null;
};

const execute = require('./SCHEMA_TITLE.runtime');

module.exports = {
  name: 'SCHEMA_TITLE',
  category: 'Schema',
  title: 'Get schema title',
  description: 'Returns a human-readable title text for the given schema, or null if the schema does not have any title text.',
  parameters: [{
    type: 'schema',
    title: 'Schema',
    description: 'Schema from which to get the title text',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'SCHEMA_TITLE({ type: "number", title: "Year" })', result: 'Year' },
    { expression: 'SCHEMA_TITLE({ type: "number" })', result: null },
  ],
};

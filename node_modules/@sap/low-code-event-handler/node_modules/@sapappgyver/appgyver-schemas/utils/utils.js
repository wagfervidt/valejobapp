"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isEmpty = exports.fromPairs = exports.pick = exports.mapValues = void 0;
var fromPairs = function (pairs) { return pairs.reduce(function (acc, _a) {
    var _b;
    var key = _a[0], value = _a[1];
    return (__assign(__assign({}, acc), (_b = {}, _b[key] = value, _b)));
}, {}); };
exports.fromPairs = fromPairs;
var mapValues = function (mapper, obj) {
    var entries = Object.entries(obj);
    return fromPairs(entries.map(function (_a) {
        var key = _a[0], value = _a[1];
        return [key, mapper(value, key)];
    }));
};
exports.mapValues = mapValues;
var pick = function (keys, obj) {
    var entries = Object.entries(obj);
    return fromPairs(entries.filter(function (_a) {
        var key = _a[0];
        return keys.includes(key);
    }));
};
exports.pick = pick;
var isEmpty = function (value) {
    if (Array.isArray(value))
        return !value.length;
    return !Object.entries(value).length;
};
exports.isEmpty = isEmpty;

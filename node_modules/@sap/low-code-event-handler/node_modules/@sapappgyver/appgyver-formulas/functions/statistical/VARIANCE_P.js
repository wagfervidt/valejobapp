const execute = require('./VARIANCE_P.runtime');

module.exports = {
  name: 'VARIANCE_P',
  category: 'Statistical',
  title: 'Variance of the population',
  description: 'Given a list of numbers as population, calculates the uncorrected variance.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'List of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    {
      context: {
        numbers: [2, 4, 6],
      },
      expression: 'VARIANCE_P(numbers)',
      result: 2.66666667,
    },
  ],
};

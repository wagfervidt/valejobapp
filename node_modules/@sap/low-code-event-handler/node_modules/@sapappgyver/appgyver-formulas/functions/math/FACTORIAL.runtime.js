module.exports = ([num]) => {
  if (num == null || num < 0) {
    return null;
  }
  if (typeof num === 'number' && !Number.isNaN(num)) {
    let result = 1;
    for (let i = 2; i <= num; i += 1) {
      result *= i;
    }
    return result;
  }
  return null;
};

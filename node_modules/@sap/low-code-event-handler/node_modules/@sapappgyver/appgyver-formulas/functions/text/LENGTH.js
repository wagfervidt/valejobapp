const execute = require('./LENGTH.runtime');

module.exports = {
  name: 'LENGTH',
  category: 'Text',
  title: 'Length of a text',
  description: 'Returns the number of characters in the given text.\n\nFor measuring the length for a list, use `COUNT` instead.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text whose length to return',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'LENGTH("Foobar")', result: 6 },
    { expression: 'LENGTH("")', result: 0 },
  ],
};

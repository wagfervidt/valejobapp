const execute = require('./JOIN.runtime');

module.exports = {
  name: 'JOIN',
  category: 'Text',
  title: 'Joins a list of texts',
  description: 'Joins a list of component texts into a single concatenated text, with each component text separated by the optional separator parameter.',
  parameters: [{
    title: 'Values',
    description: 'List of text values',
    inversible: true,
    type: 'array',
    items: {
      type: 'string',
    },
  }, {
    type: 'string',
    title: 'Separator',
    description: 'Separator for texts',
  }],
  maxInversions: 1,
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'JOIN(["a", "b", "c"], ", ")', result: 'a, b, c' },
    { expression: 'JOIN([], "")', result: '' },
  ],
};

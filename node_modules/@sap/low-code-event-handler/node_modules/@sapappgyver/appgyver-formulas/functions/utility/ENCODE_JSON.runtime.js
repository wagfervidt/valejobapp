module.exports = ([value, prettyprintIndent]) => {
  const spaces = prettyprintIndent || 0;
  if (typeof (spaces === Number) && Number.isFinite(spaces) && spaces >= 0) {
    return JSON.stringify(value, null, spaces);
  }
  return null;
};

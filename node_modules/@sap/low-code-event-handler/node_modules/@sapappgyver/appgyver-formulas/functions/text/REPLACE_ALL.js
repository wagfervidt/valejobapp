const execute = require('./REPLACE_ALL.runtime');

module.exports = {
  name: 'REPLACE_ALL',
  category: 'Text',
  title: 'Replace all found slices of texts',
  description: 'Returns a new text where all occurrences of the given text are replaced with the given replacement text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Original text',
  }, {
    type: 'string',
    title: 'Slice of text',
    description: 'Slice of text to find',
  }, {
    type: 'string',
    title: 'New slice of text',
    description: 'Slice of text to replace the old ones',
  },
  ],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'REPLACE_ALL("foo bar baz", "bar", "baz")', result: 'foo baz baz' },
    { expression: 'REPLACE_ALL("foo foo baz", "foo", "bar")', result: 'bar bar baz' },
    { expression: 'REPLACE_ALL("foo bar", "baz", "bar")', result: 'foo bar' },
  ],
};

const moment = require('moment-timezone');
const { formatMoment } = require('../../datetime');

module.exports = ([format], { timestamp }) => {
  const now = moment(timestamp).utc();
  return formatMoment(now, format);
};

module.exports = (inv) => {
  const { result } = inv;
  if (typeof result !== 'number') {
    throw new Error('Cannot invert the opposite of a non-number value');
  }
  return inv.invertTo(-result);
};

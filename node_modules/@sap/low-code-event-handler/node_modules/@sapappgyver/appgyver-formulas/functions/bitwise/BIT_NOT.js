const execute = require('./BIT_NOT.runtime');

module.exports = {
  name: 'BIT_NOT',
  category: 'Bitwise',
  title: 'Bitwise NOT',
  description: 'Returns the bitwise NOT of the given value, inverting its bits.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number A',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'BIT_NOT(9)', result: -10 },
    { expression: 'BIT_NOT(10)', result: -11 },
  ],
};

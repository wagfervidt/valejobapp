module.exports = ([item, count], { checkTimeout }) => {
  const result = [];
  if (typeof count === 'number' && Number.isFinite(count)) {
    for (let i = 0; i < count; i += 1) {
      result.push(item);
      checkTimeout();
    }
  }
  return result;
};

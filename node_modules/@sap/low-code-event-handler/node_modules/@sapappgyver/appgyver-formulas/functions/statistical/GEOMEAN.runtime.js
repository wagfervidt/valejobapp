module.exports = ([array]) => {
  if (!Array.isArray(array)) {
    return null;
  }
  const arrayValid = array.filter(item => (typeof item === 'number' && Number.isFinite(item)));
  if (!arrayValid.length) {
    return null;
  }
  const product = arrayValid.reduce((total, item) => total * item, 1);
  const result = (product > 0) ? product ** (1 / arrayValid.length) : null;
  return Number.isFinite(result) ? result : null;
};

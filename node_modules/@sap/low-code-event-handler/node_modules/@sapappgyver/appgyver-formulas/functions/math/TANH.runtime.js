module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const tanh = Math.tanh(num);
    if (Number.isFinite(tanh)) {
      return tanh;
    }
  }
  return null;
};

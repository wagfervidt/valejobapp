const execute = require('./UPPERCASE.runtime');

module.exports = {
  name: 'UPPERCASE',
  category: 'Text',
  title: 'Convert letters to upper case',
  description: 'Converts all characters of the given text to upper-case letters.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to convert to upper case',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'UPPERCASE("foobar")', result: 'FOOBAR' },
    { expression: 'UPPERCASE("Foo Bar")', result: 'FOO BAR' },
  ],
};

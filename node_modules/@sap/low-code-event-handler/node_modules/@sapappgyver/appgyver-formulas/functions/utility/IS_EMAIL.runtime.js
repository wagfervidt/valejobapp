const { formats: { email: emailFormat } } = require('../../formatting');

module.exports = ([value]) => {
  if (typeof value === 'string') {
    return emailFormat.matches(value);
  }
  return false;
};

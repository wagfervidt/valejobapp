module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const result = Math.asin(1 / num);
    if (Number.isFinite(result)) {
      return result;
    }
  }
  return null;
};

const execute = require('./COUNT.runtime');

module.exports = {
  name: 'COUNT',
  category: 'List',
  title: 'Size of a list',
  description: `Returns the number of items in the given list.

For measuring the length for a text, use \`LENGTH\` instead.`,
  parameters: [{
    type: 'array',
    title: 'List',
    description: 'List whose items to count',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'COUNT([1, 2, 3])', result: 3 },
    { expression: 'COUNT([])', result: 0 },
  ],
};

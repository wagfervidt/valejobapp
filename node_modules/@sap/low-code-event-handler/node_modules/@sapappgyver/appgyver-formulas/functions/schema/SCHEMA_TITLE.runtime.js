const { getSchemaAnnotation } = require('@sapappgyver/appgyver-schemas');

module.exports = ([schema]) => {
  const title = getSchemaAnnotation(schema, 'title');
  return typeof title === 'string' ? title : null;
};

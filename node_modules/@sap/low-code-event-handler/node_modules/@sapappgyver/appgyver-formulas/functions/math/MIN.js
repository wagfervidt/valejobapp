const execute = require('./MIN.runtime');

module.exports = {
  name: 'MIN',
  category: 'Math',
  title: 'Lowest number in list',
  description: 'Given a list of numbers, returns the smallest number in the list.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'List of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'MIN([100, 3, 20])', result: 3 },
    { expression: 'MIN([])', result: null },
  ],
};

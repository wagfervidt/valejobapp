module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const ePowerNum = Math.E ** (2 * num);
    const result = (ePowerNum - 1) / (ePowerNum + 1);
    return Number.isFinite(result) ? result : null;
  }
  return null;
};

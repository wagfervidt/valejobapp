const execute = require('./VARIANCE.runtime');

module.exports = {
  name: 'VARIANCE',
  category: 'Statistical',
  title: 'Calculate variance of a list',
  description: 'Given a list of numbers as sample, calculates the unbiased variance.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'List of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    {
      context: {
        numbers: [2, 4, 6],
      },
      expression: 'VARIANCE(numbers)',
      result: 4,
    },
    {
      context: {
        singleNumber: [1],
      },
      expression: 'VARIANCE(singleNumber)',
      result: 0,
    },
  ],
};

module.exports = ([array]) => {
  if (!Array.isArray(array)) {
    return null;
  }
  const sumSquares = array.reduce((sum, value) => sum + (typeof value === 'number' ? (value ** 2) : 0), 0);
  const result = Math.sqrt(sumSquares);
  return Number.isFinite(result) ? result : null;
};

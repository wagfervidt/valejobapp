module.exports = ([array, init, func]) => {
  if (!Array.isArray(array) || typeof func !== 'function') {
    return null;
  }
  return array.reduce((accumulator, item, index) => func(accumulator, item, index), init);
};

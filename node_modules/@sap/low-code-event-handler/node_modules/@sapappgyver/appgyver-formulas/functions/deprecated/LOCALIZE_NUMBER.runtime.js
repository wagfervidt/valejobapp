module.exports = ([value, locale, options]) => {
  if (value == null) {
    return null;
  }
  if (typeof value === 'number') {
    try {
      const opt = options || {};
      return value.toLocaleString(locale, opt);
    } catch (e) {
      return null;
    }
  }
  return null;
};

const GCD = (a, b) => {
  let x = Math.abs(a);
  let y = Math.abs(b);
  while (y) {
    const t = y;
    y = x % y;
    x = t;
  }
  return x;
};

const GCDArray = (numbers) => {
  let a = numbers[0];
  numbers.forEach((number) => {
    a = GCD(a, number);
  });
  return a;
};

module.exports = ([array]) => {
  if (!Array.isArray(array)) {
    return null;
  }
  const validArray = array.filter(item => (typeof item === 'number' && Number.isFinite(item))).map(item => (Math.floor(item)));
  if (validArray.length) {
    return GCDArray(validArray);
  }
  return null;
};

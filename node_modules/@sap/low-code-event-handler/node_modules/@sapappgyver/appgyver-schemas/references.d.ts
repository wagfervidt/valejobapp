import { NullableSchema, Schema } from './schemas';

export function idenfitySchemas(modelSchema: Schema, actualSchema: Schema): Record<string, Schema>;
export function resolveSchemaReferences(
  schema: Schema, references: Record<string, Schema>, defaultSchema: NullableSchema
): Schema | null;

const execute = require('./FLOOR.runtime');

module.exports = {
  name: 'FLOOR',
  category: 'Math',
  title: 'Rounds a number down to integer',
  description: 'Returns the given number, rounded down to the nearest integer. If the given number is an integer, returns itself.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to round',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'FLOOR(4.1)', result: 4 },
    { expression: 'FLOOR(4.9)', result: 4 },
  ],
};

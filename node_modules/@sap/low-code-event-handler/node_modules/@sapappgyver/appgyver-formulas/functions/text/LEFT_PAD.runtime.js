module.exports = ([string, width, chars]) => {
  if (typeof string !== 'string') {
    return null;
  }
  if (typeof width !== 'number' || Number.isNaN(width)) {
    return null;
  }
  const spacer = (typeof chars === 'string') ? chars : ' ';
  return string.padStart(width, spacer);
};

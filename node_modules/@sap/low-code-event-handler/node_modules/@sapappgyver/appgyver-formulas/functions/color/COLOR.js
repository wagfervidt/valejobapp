const execute = require('./COLOR.runtime');

module.exports = {
  name: 'COLOR',
  category: 'Color',
  title: 'Convert to color',
  description: 'Parse the given text as a color and return a text representing a valid color, or `null` if the color cannot be parsed.',
  parameters: [{
    title: 'Text',
    description: 'Text to parse as a color',
    type: 'string',
  }],
  analyze: () => ({ schema: { type: 'string', format: 'color' } }),
  execute,
  examples: [
    { expression: 'COLOR("#ff0")', result: 'rgb(255,255,0)' },
    { expression: 'COLOR("rgb(0, 20, 40)")', result: 'rgb(0,20,40)' },
    { expression: 'COLOR("hsl(80, 20%, 40%)")', result: 'rgb(109,122,82)' },
    { expression: 'COLOR("red")', result: 'rgb(255,0,0)' },
  ],
};

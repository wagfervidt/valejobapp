const { compareDateTimes } = require('../../datetime');

module.exports = ([date1, date2, precision]) => {
  const result = compareDateTimes(date1, date2, precision);
  return result != null && result < 0;
};

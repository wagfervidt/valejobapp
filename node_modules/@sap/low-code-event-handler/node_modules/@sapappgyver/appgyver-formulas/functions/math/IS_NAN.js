const execute = require('./IS_NAN.runtime');

module.exports = {
  name: 'IS_NAN',
  category: 'Math',
  title: 'Check if the number is NaN',
  description: 'Returns `true` if the given number is `NaN`, a special "not a number" symbol. For example, dividing any number by 0 results in `NaN`.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to check',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'IS_NAN(NaN)', result: true },
    { expression: 'IS_NAN(123)', result: false },
  ],
};

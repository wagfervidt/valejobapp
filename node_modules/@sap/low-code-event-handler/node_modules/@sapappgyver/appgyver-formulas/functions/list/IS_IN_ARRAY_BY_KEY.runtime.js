const isEmpty = require('../utility/IS_EMPTY.runtime');
const findByKey = require('./FIND_BY_KEY.runtime');

module.exports = ([array, key, item]) => {
  const foundItem = findByKey([array, key, item]);
  return !isEmpty([foundItem]);
};

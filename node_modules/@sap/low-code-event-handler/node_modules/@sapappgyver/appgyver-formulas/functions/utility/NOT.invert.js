module.exports = (inv) => {
  const { params: [value], result } = inv;
  if (typeof result !== 'boolean' || (typeof value !== 'boolean' && value != null)) {
    throw new Error('Cannot invert negation of a non-boolean value');
  }
  return inv.invertTo(!result);
};

const execute = require('./IS_EMAIL.runtime');

module.exports = {
  name: 'IS_EMAIL',
  category: 'Utility',
  title: 'Check if given text is a email',
  description: 'Returns `true` if the given text is a valid email address, otherwise returns `false`.',
  parameters: [{
    title: 'Value',
    description: 'Value to check',
    type: 'string',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'IS_EMAIL("test@mail.com")', result: true },
    { expression: 'IS_EMAIL("@mail.com")', result: false },
  ],
};

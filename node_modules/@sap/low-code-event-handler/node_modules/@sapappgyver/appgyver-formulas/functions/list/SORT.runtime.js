const { sort } = require('@sapappgyver/appgyver-schemas/utils/array');

module.exports = ([array, order = 'asc']) => {
  if (!Array.isArray(array)) {
    return null;
  }
  return sort(array, order, null, false);
};

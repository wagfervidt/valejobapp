const execute = require('./VARIANCE_B.runtime');

module.exports = {
  name: 'VARIANCE_B',
  category: 'Statistical',
  title: 'Biased variance',
  description: 'Given a list of numbers, calculates the biased variance.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'List of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    {
      context: {
        numbers: [2, 4, 6],
      },
      expression: 'VARIANCE_B(numbers)',
      result: 2,
    },
  ],
};

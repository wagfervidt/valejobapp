const execute = require('./SUM.runtime');

module.exports = {
  name: 'SUM',
  category: 'Math',
  title: 'Calculate sum of an array',
  description: 'Given an array of numbers, calculates their sum. If the array is empty, returns 0.',
  parameters: [{
    type: 'array',
    title: 'Values',
    description: 'Array of numeric values',
    items: {
      type: 'number',
    },
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'SUM([10, 20, 30])', result: 60 },
    { expression: 'SUM([])', result: 0 },
  ],
};

const execute = require('./BOOLEAN.runtime');

module.exports = {
  name: 'BOOLEAN',
  category: 'Utility',
  title: 'Convert value to true/false',
  description: 'Converts the given value to a true/false. This conversion is equivalent to JavaScript `!!value`.',
  parameters: [{
    title: 'Value',
    description: 'Value to convert',
    anyOf: [{ type: 'string' }, { type: 'number' }, { type: 'boolean' }],
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'BOOLEAN("123")', result: true },
    { expression: 'BOOLEAN(123)', result: true },
    { expression: 'BOOLEAN("")', result: false },
  ],
};

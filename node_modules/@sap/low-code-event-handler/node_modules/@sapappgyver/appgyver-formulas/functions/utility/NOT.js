const execute = require('./NOT.runtime');

module.exports = {
  name: 'NOT',
  category: 'Utility',
  title: 'Logical negation',
  description: 'Returns `true` if the given parameter is `false`, otherwise returns `false`. Equivalent to the `!` operator.',
  parameters: [{
    type: 'boolean',
    title: 'Value',
    description: 'True/false value to negate',
    inversible: true,
  }],
  maxInversions: 1,
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'NOT(false)', result: true },
    { expression: 'NOT(true)', result: false },
  ],
};

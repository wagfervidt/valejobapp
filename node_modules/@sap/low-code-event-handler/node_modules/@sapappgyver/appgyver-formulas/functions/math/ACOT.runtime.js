module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const acot = (Math.PI / 2) - Math.atan(num);
    if (Number.isFinite(acot)) {
      return acot;
    }
  }
  return null;
};

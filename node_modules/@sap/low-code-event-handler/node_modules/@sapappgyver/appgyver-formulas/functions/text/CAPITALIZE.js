const execute = require('./CAPITALIZE.runtime');

module.exports = {
  name: 'CAPITALIZE',
  category: 'Text',
  title: 'Capitalize text',
  description: 'Capitalizes each word in a specified text so that they start with a upper-case letter, leaving all other letters lower-case.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to convert to capitalize case',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'CAPITALIZE("foobar")', result: 'Foobar' },
    { expression: 'CAPITALIZE("Foo Bar")', result: 'Foo Bar' },
    { expression: 'CAPITALIZE("fOoO baAaR")', result: 'Fooo Baaar' },
  ],
};

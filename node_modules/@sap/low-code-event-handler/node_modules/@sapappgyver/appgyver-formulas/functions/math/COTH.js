const execute = require('./COTH.runtime');

module.exports = {
  name: 'COTH',
  category: 'Math',
  title: 'Calculate hyperbolic cotangent',
  description: 'Given an angle in radians, calculates its hyberbolic cotangent.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Angle in radians to calculate cotangent for',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'COTH(PI() / 4)', result: 1.52486862 },
    { expression: 'COTH(PI() / 2)', result: 1.09033141 },
    { expression: 'COTH(PI() / 6)', result: 2.08128336 },
  ],
};

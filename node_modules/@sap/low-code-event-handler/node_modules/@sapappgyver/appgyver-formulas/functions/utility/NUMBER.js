const execute = require('./NUMBER.runtime');

module.exports = {
  name: 'NUMBER',
  category: 'Utility',
  title: 'Parse a number',
  description: 'Converts the value to a number, if it is not already a number. Numeric texts are parsed. Any other values, and invalid texts, result in `null` value.',
  parameters: [{
    title: 'Value',
    description: 'Value to convert to a number',
    anyOf: [{
      type: 'string',
    }, {
      type: 'number',
    }],
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'NUMBER("123")', result: 123 },
    { expression: 'NUMBER("-0896")', result: -896 },
    { expression: 'NUMBER(-4.8)', result: -4.8 },
  ],
};

const execute = require('./CONTAINS.runtime');

module.exports = {
  name: 'CONTAINS',
  category: 'Text',
  title: 'Check if a text contains a slice of text',
  description: 'Returns `true` if the given text contains the given slice of text, otherwise returns `false`.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Text to test',
  }, {
    type: 'string',
    title: 'Pattern',
    description: 'Slice of text to find',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'CONTAINS("Foo Bar", "Foo")', result: true },
    { expression: 'CONTAINS("Foo Bar", "foo")', result: false },
    { expression: 'CONTAINS("Foo Bar", "o B")', result: true },
  ],
};

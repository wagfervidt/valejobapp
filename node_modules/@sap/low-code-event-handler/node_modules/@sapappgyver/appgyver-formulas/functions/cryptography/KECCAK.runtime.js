const SHA3 = require('crypto-js/sha3');

module.exports = ([value, bits]) => {
  if (typeof value === 'string') {
    if (bits === 224
      || bits === 256
      || bits === 384
      || bits === 512) {
      return SHA3(value, { outputLength: bits }).toString();
    }
  }
  return null;
};

const normDist = require('./NORM_DIST.runtime');

module.exports = ([x]) => {
  if (
    typeof x === 'number'
      && Number.isFinite(x)
  ) {
    const result = normDist([x, 0, 1, false]);
    return Number.isFinite(result) ? result : null;
  }
  return null;
};

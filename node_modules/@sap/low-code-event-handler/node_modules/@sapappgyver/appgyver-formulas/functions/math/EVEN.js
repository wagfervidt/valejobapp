const execute = require('./EVEN.runtime');

module.exports = {
  name: 'EVEN',
  category: 'Math',
  title: 'Rounds a number up to the nearest even integer',
  description: 'Returns the given number, rounded up to the nearest even integer. If the given number is an even integer, returns itself.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to round',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'EVEN(1)', result: 2 },
    { expression: 'EVEN(-1)', result: -2 },
    { expression: 'EVEN(3)', result: 4 },
    { expression: 'EVEN(-12.3)', result: -14 },
    { expression: 'EVEN(12.3)', result: 14 },
  ],
};

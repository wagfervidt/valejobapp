const execute = require('./COT.runtime');

module.exports = {
  name: 'COT',
  category: 'Math',
  title: 'Calculate cotangent',
  description: 'Given an angle in radians, calculates its cotangent.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Angle in radians to calculate cotangent for',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'COT(PI() / 4)', result: 1 },
    { expression: 'COT(PI() / 2)', result: 0 },
    { expression: 'COT(PI() / 6)', result: 1.73205081 },
  ],
};

module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const cotan = 1 / Math.tan(num);
    if (Number.isFinite(cotan)) {
      return cotan;
    }
  }
  return null;
};

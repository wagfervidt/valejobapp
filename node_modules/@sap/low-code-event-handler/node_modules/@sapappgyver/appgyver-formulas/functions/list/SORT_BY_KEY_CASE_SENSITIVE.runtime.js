const { sort } = require('@sapappgyver/appgyver-schemas/utils/array');

module.exports = ([array, key, order = 'asc']) => {
  if (!Array.isArray(array) || key == null) {
    return null;
  }
  return sort(array, order, key, true);
};

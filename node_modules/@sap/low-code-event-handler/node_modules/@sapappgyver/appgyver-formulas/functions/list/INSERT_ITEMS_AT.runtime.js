const SPLICE = require('./SPLICE.runtime');

module.exports = ([array, index, newItems]) => {
  if (!Array.isArray(array) || !Array.isArray(newItems) || !Number.isFinite(index)) {
    return null;
  }
  const intIndex = parseInt(index, 10);

  return SPLICE([[...array], intIndex, 0, newItems]);
};

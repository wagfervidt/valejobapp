const { invertLookup } = require('../../utils/object');

module.exports = (inv) => {
  const { params: [object, prop], result: propValue } = inv;
  return inv.invertTo(invertLookup(object, prop, propValue));
};

const execute = require('./DECODE_BASE64.runtime');

module.exports = {
  name: 'DECODE_BASE64',
  category: 'Utility',
  title: 'Convert Base64 to text',
  description: 'Converts a given Base64 encoded text to UTF-8 text.',
  parameters: [{
    title: 'Value',
    description: 'Value to convert',
    type: 'string',
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'DECODE_BASE64("Zm9vYmFy")', result: 'foobar' },
    { expression: 'DECODE_BASE64("w6TDpMOk")', result: 'äää' },
  ],
};

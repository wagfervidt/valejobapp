module.exports = ([num, precision]) => {
  const prec = Math.trunc(precision) || 0;
  if (typeof num === 'number' && Number.isFinite(num) && typeof prec === 'number' && Number.isFinite(prec)) {
    return Math.round(num * (10 ** prec)) / (10 ** prec);
  }
  return null;
};

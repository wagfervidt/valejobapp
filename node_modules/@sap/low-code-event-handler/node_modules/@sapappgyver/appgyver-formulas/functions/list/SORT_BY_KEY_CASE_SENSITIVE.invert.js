const { invertSort } = require('../../utils/array');

module.exports = (inv) => {
  const { params: [array, key, order], result } = inv;
  if (key == null) {
    throw new Error('Cannot invert with a missing key');
  }
  return inv.invertTo(invertSort(array, result, order, key, true));
};

const execute = require('./SIN.runtime');

module.exports = {
  name: 'SIN',
  category: 'Math',
  title: 'Calculate sine',
  description: 'Given an angle in radians, calculates its sine.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Angle in radians to calculate sine for',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'SIN(0)', result: 0 },
    { expression: 'SIN(PI() / 6)', result: 0.5 },
    { expression: 'SIN(PI() / 2)', result: 1 },
  ],
};

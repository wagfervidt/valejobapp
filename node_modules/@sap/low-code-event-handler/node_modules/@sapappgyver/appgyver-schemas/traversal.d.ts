import { NullableSchema, Schema } from './schemas';

export function traverseSchema(schema: NullableSchema, iteratee: (schema: Schema) => Schema): Schema;
export function replaceSchemaTypes(schema: NullableSchema, replacements: Record<string, Schema>): Schema;

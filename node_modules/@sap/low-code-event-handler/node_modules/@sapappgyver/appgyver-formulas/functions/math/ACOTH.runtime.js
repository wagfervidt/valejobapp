module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    if (num >= -1 && num <= 1) {
      return null;
    }
    const acoth = Math.atanh(1 / num);
    if (Number.isFinite(acoth)) {
      return acoth;
    }
  }
  return null;
};

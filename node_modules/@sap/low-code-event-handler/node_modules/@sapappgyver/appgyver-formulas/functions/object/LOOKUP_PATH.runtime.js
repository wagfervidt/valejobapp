module.exports = ([object, path]) => {
  if (object == null || typeof object !== 'object') {
    return undefined;
  }

  if (!Array.isArray(path)) {
    return undefined;
  }

  return path.reduce((result, key) => (typeof result === 'object' && result !== null ? result[key] : undefined), object);
};

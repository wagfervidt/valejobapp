const execute = require('./LOCAL_TIMEZONE.runtime');

module.exports = {
  name: 'LOCAL_TIMEZONE',
  category: 'Date',
  title: 'Get local timezone',
  description: 'Returns the current local timezone of the device running the app.',
  parameters: [],
  deterministic: false,
  analyze: () => ({
    schema: {
      type: 'string',
      examples: ['Europe/Helsinki', 'US/Central'],
    },
  }),
  execute,
  examples: [
    { expression: 'LOCAL_TIMEZONE()', result: 'US/Central' },
  ],
};

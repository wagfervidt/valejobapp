module.exports = ([array, separatorParam]) => {
  if (array == null) {
    return null;
  }
  let separator = separatorParam;
  if (separator == null) {
    separator = '';
  }
  return array.join(separator);
};

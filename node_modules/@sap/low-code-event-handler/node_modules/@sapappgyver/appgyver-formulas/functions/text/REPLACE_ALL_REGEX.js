const execute = require('./REPLACE_ALL_REGEX.runtime');

module.exports = {
  name: 'REPLACE_ALL_REGEX',
  category: 'Text',
  title: 'Replace all occurrences by regex',
  description: 'Returns a new text where all occurrences for the given regular expression are replaced with the given replacement text.',
  parameters: [{
    type: 'string',
    title: 'Text',
    description: 'Original text',
  }, {
    type: 'string',
    title: 'Pattern',
    description: 'Pattern to find',
  }, {
    type: 'string',
    title: 'New slice of text',
    description: 'Slice of text to replace',
  },
  ],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'REPLACE_ALL_REGEX("foo bar", "[a-zA-Z]+", "xxxx")', result: 'xxxx xxxx' },
    { expression: 'REPLACE_ALL_REGEX("foo foo foo", "^foo", "bar")', result: 'bar foo foo' },
    { expression: 'REPLACE_ALL_REGEX("foo foo baz", "\\\\s", "-")', result: 'foo-foo-baz' },
  ],
};

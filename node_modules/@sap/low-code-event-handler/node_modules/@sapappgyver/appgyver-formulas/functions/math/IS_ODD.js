const execute = require('./IS_ODD.runtime');

module.exports = {
  name: 'IS_ODD',
  category: 'Math',
  title: 'Check if the number is odd',
  description: 'Returns `true` if the given number is odd, `false` if it\'s odd.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to check',
  }],
  analyze: () => ({ schema: { type: 'boolean' } }),
  execute,
  examples: [
    { expression: 'IS_ODD(0)', result: false },
    { expression: 'IS_ODD(1)', result: true },
    { expression: 'IS_ODD(2)', result: false },
  ],
};

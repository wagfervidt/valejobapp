const { formats: { url: urlFormat } } = require('../../formatting');

module.exports = ([param]) => {
  if (typeof param === 'string') {
    if (urlFormat.matches(param)) {
      return param;
    }
  }
  return null;
};

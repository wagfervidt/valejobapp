const execute = require('./SQRT.runtime');

module.exports = {
  name: 'SQRT',
  category: 'Math',
  title: 'Square root of number',
  description: 'Calculate the square root for the given number.',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Number to calculate',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'SQRT(4)', result: 2 },
    { expression: 'SQRT(0.25)', result: 0.5 },
  ],
};

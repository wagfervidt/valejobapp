module.exports = ([numX, numY]) => {
  if (typeof numX === 'number' && Number.isFinite(numX)
    && typeof numY === 'number' && Number.isFinite(numY)) {
    const atan = Math.atan2(numY, numX);
    if (Number.isFinite(atan)) {
      return atan;
    }
    return null;
  }
  return null;
};

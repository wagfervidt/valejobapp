const execute = require('./TAN.runtime');

module.exports = {
  name: 'TAN',
  category: 'Math',
  title: 'Tangent',
  description: 'Given an angle in radians, calculates its tangent',
  parameters: [{
    type: 'number',
    title: 'Number',
    description: 'Angle in radians to calculate tangent for',
  }],
  analyze: () => ({ schema: { type: 'number' } }),
  execute,
  examples: [
    { expression: 'TAN(0)', result: 0 },
    { expression: 'TAN(PI() / 4)', result: 1 },
    { expression: 'TAN(PI() / 6)', result: 0.57735027 },
  ],
};

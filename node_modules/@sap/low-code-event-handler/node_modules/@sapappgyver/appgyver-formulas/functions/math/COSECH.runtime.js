module.exports = ([num]) => {
  if (typeof num === 'number' && Number.isFinite(num)) {
    const cosech = 1 / Math.sinh(num);
    if (Number.isFinite(cosech)) {
      return cosech;
    }
  }
  return null;
};

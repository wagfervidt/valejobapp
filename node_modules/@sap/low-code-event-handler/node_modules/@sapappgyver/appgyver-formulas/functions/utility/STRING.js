const execute = require('./STRING.runtime');

module.exports = {
  name: 'STRING',
  category: 'Utility',
  title: 'Convert the value to a text',
  description: 'Converts the given value to a text. If the value is a number, returns its text representation, possibly including decimals. True/false values are converted to either `"true"` or `"false"`. A `null` value is converted to an empty text.',
  parameters: [{
    title: 'Value',
    description: 'Value to stringify',
    anyOf: [{ type: 'string' }, { type: 'number' }, { type: 'boolean' }],
  }],
  analyze: () => ({ schema: { type: 'string' } }),
  execute,
  examples: [
    { expression: 'STRING(123.456)', result: '123.456' },
    { expression: 'STRING(true)', result: 'true' },
    { expression: 'STRING("hello")', result: 'hello' },
  ],
};
